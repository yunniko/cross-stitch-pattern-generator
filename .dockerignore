# Keeps the build context small: the Dockerfile's `COPY . .` would otherwise send these to the daemon.
node_modules
.next
.git
test-results
playwright-report
coverage
*.tsbuildinfo
.env*
docs/reviews/*assets*
