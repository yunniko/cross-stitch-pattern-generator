# Keeps the build context small: the Dockerfile's `COPY . .` would otherwise send these to the daemon.
node_modules
.next
.next-*
dist
.git
test-results
playwright-report
coverage
*.tsbuildinfo
.env*
docs/reviews/*assets*
# Cargo build output: gigabytes of artifacts the image builds fresh anyway.
rust/target
