# Decisions — index

One file per significant decision (`Dnnn-<slug>.md`, template and size cap in
`COMPANY/STANDARDS.md` → Documentation). Append-only: a reversal is a new file
that names the old one. D1–D96 predate this folder and still live inline in
`HANDOVER.md` until G-031 M5 migrates them.

- D097 — The pattern deserializer validates every field, not just grid geometry — active
- D098 — Project autosave moves to IndexedDB, photo stored once by content hash — active
- D099 — A failed auto-restore shows a banner with an on-demand report, not a page-load download — active
- D100 — Playwright runs against `next build && next start` on its own port — active
- D101 — Keyboard shortcuts live in a hook that reads state through a ref — active
- D102 — Brush, Move and Select drags redraw only what changed — active
